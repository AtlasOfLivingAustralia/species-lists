apiVersion: v1
kind: Service
metadata:
  name: svc-{{ include "ala-species-lists.fullname" . }}
  labels:
    {{- include "ala-species-lists.labels" . | nindent 4 }}
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-additional-resource-tags: {{ include "ala-species-lists.resourceTags" . | quote }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort}}
      protocol: TCP
  selector:
    {{- include "ala-species-lists.selectorLabels" . | nindent 4 }}