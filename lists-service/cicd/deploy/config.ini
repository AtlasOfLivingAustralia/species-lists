[DEFAULT]
PRODUCT_COMPONENT = deploy
PIPELINE_STACK_NAME = ala-${PRODUCT_NAME}-${PRODUCT_COMPONENT}-pipeline-${ENVIRONMENT}
BASE_STACK_NAME = ala-${PRODUCT_NAME}-base-${ENVIRONMENT}
DATABASE_STACK_NAME = ala-${PRODUCT_NAME}-database-${ENVIRONMENT}
HELM_RELEASE_NAME = ala-species-lists-${ENVIRONMENT}
AUTO_DEPLOY = true
SLACK_DEPLOY_NOTIFICATION = true
SLACK_ALERT_CHANNEL = zabbix-alerts

# DocDB
ADMIN_USER = adminuser

BIE_BASE_URL = https://bie-ws.ala.org.au/ws
IMAGES_BASE_URL = https://images.ala.org.au
NAMEMATCHING_BASE_URL = https://namematching-ws.ala.org.au/
USERDETAILS_BASE_URL = https://api.ala.org.au/userdetails

[development]
# code pipeline
PIPELINE_STACK_NAME = ala-${PRODUCT_NAME}-${PRODUCT_COMPONENT}-pipeline-${CLEAN_BRANCH}
BASE_STACK_NAME = ala-${PRODUCT_NAME}-base-${CLEAN_BRANCH}
DATABASE_STACK_NAME = ala-${PRODUCT_NAME}-database-${CLEAN_BRANCH}
HELM_RELEASE_NAME = ala-species-lists-${CLEAN_BRANCH}

AUTO_DEPLOY = false

COGNITO_POOL_ID = ap-southeast-2_OOXU9GW39
COGNITO_APP_NAME = species-lists-ui-test

USERDETAILS_BASE_URL = https://api.test.ala.org.au/userdetails

HOSTED_ZONE = dev.ala.org.au
SUB_DOMAIN = ${PRODUCT_NAME}-${CLEAN_BRANCH}-ws

[testing]

USERDETAILS_BASE_URL = https://api.test.ala.org.au/userdetails

HOSTED_ZONE = test.ala.org.au
SUB_DOMAIN = ${PRODUCT_NAME}-ws

[staging]

HOSTED_ZONE = ala.org.au
SUB_DOMAIN = ${PRODUCT_NAME}-staging-ws

[production]

HOSTED_ZONE = ala.org.au
SUB_DOMAIN = ${PRODUCT_NAME}-ws

AUTO_DEPLOY = false
SLACK_DEPLOY_NOTIFICATION = true
SLACK_ALERT_CHANNEL = deployments